# TO-DO-APP
This is a full-stack ToDo Application built using HTML, CSS, JavaScript (Frontend) and Node.js, Express, MongoDB (Backend). Users can sign up, log in, and manage their personal tasks — including adding, updating, marking as completed, and deleting todos.  The frontend is deployed on Vercel, and the backend is deployed on Render.
